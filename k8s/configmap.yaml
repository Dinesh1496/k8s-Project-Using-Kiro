apiVersion: v1
kind: ConfigMap
metadata:
  name: flask-app-config
  namespace: flask-app
  labels:
    app: flask-app
    component: config
data:
  APP_ENV: "production"
  APP_VERSION: "1.0.0"
  PORT: "5000"
  LOG_LEVEL: "info"
  GUNICORN_WORKERS: "2"
  GUNICORN_THREADS: "2"
